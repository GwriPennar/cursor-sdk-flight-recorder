# Tiny Example Repo

A minimal public-safe repository for the Cursor SDK Flight Recorder demo.

- `hello.py` — simple `greet()` function
- `tests/test_hello.py` — pytest coverage

Use this path as the default repo when running live or demo modes.
